CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT(algorithm)
SET (PROJECT_ROOT_DIR ${PROJECT_SOURCE_DIR}/..)
SET (SRC_FOLDER ${PROJECT_ROOT_DIR}/src)

SET (CMAKE_C_COMPILER "g++")
SET (CMAKE_C_FLAGS "-g -O0 -fPIC -fpermissive")
SET (CMAKE_CXX_FLAGS "-std=c++0x -fpermissive -g -O0 -fPIC")
#SET (CMAKE_EXE_LINKER_FLAGS "-ldl -lz")

FILE (GLOB_RECURSE TEST_MAIN_SRC_CPP "${SRC_FOLDER}/*.cpp")
FILE (GLOB_RECURSE TEST_MAIN_SRC_C "${SRC_FOLDER}/*.c")
#SET (TEST_MAIN_SRC ${TEST_MAIN_SRC_CPP} ${TEST_MAIN_SRC_C})
SET (TEST_MAIN_SRC ${TEST_MAIN_SRC_C} ${TEST_MAIN_SRC_CPP})

INCLUDE_DIRECTORIES (${SRC_FOLDER})
INCLUDE_DIRECTORIES (${PROJECT_ROOT_DIR})
#INCLUDE_DIRECTORIES (${PROJECT_ROOT_DIR}/thirdparty/libspatialite-4.3.0a/include)


#LINK_DIRECTORIES (${PROJECT_ROOT_DIR}/thirdparty/libspatialite-4.3.0a/lib)

MESSAGE(${SRC_FOLDER})
ADD_EXECUTABLE (algorithm ${TEST_MAIN_SRC})

TARGET_LINK_LIBRARIES (algorithm gtest)
TARGET_LINK_LIBRARIES (algorithm pthread)

